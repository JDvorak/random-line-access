{
  "name": "random-line-access",
  "version": "0.1.1",
  "description": "Random access line",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "jdvorak/random-line-access",
  "keywords": [
    ""
  ],
  "license": "MIT",
  "dependencies": {
    "binary-split": "^1.0.3",
    "pump": "^3.0.0",
    "random-access-file": "^2.0.1",
    "xtend": "^4.0.1"
  },
  "devDependencies": {
    "dependency-check": "^3.1.0",
    "istanbul": "^0.4.5",
    "standard": "^11.0.1",
    "tape": "^4.9.0"
  }
}
